# 12-开发起步与骨架-审计

> 文档类型：阶段产物审计报告
> 审计日期：2026-07-14
> 审计对象：README.md、.sln、src/LibrarySeatReservation.Web/、src/LibrarySeatReservation.Web/Migrations/、docs/11-开发前一致性总审计.md、docs/12-开发起步与骨架记录.md、docs/项目任务板与迭代记录.md
> 审计依据：docs/07、docs/08、docs/09、docs/11 + 任务板（门禁与对照）

---

## A. 总体判断

**✅ 通过（可进入下一步）**

Sprint 0 骨架搭建的 10 项任务（T17-01~T17-10）全部完成，项目已具备"能编译、能启动、能看到入口页、能完成首次建库建表和种子初始化"的完整能力。docs/11 的 3 项 P0 阻塞项已全部清零，12-骨架记录与实际工程产物一致。

---

## B. 发现的问题清单

| 编号 | 级别 | 问题描述 | 涉及文件 | 说明 |
|------|------|---------|---------|------|
| B-01 | P1 | docs/12 记录的创建命令使用 `--framework net6.0`，但实际 TargetFramework 已改为 net8.0 | docs/12 §3.1 | 记录了"先创建后修改"的过渡步骤，不阻塞，但可能造成后续阅读混淆 |
| B-02 | P1 | docs/12 中 `Views/` 目录写"9 个页面视图"，但实际已有 10+ 视图（含骨架页） | docs/12 §3.3 | Sprint 0 只完成了 2 个完整页（首页+登录），其余 7 个为骨架，但 docs/12 描述为"9 个页面视图"未区分完整/骨架 |
| B-03 | P1 | docs/12 §4 "ForeignKey 删除行为"写"前序文档说 NO ACTION，EF Core 默认 CASCADE"，但 DbContext 中实际未显式配置删除行为 | docs/12 §4, AppDbContext.cs | 当前使用 EF Core 默认行为，未在 OnModelCreating 中显式设置 `.OnDelete(DeleteBehavior.Restrict)`，与 docs/08 的设计口径不完全一致 |
| B-04 | P2 | docs/12 §4 假设项"Service 层按课程技术口径创建"，但 docs/10 和 docs/07 明确说"本期不使用 Service 层" | docs/12 §4 | 已在 docs/12 中显式标注为"用户明确指定"的偏差，不阻塞，但需记录为后续修订项 |
| B-05 | P2 | docs/12 §4 假设项"DataAccess 层按课程技术口径创建"，但 docs/07 未单独列出 DataAccess 层 | docs/12 §4 | 同上，已显式标注为偏差 |

---

## C. 必改项

无。

本阶段所有必改项（P0）已在 docs/11 的 Go/No-Go 门禁中清零，且在进入 Sprint 0 编码前完成回改：

| P0 编号 | 问题 | 回改状态 |
|---------|------|---------|
| C-S1 | .NET 版本统一为 .NET 8 | ✅ 已处理（01、03 回改） |
| C-P1 | 管理端路由统一为 `/SeatIndex` 等 | ✅ 已处理（01、04 回改） |
| C-D1 | AdminUser 补充 CreatedAt 字段 | ✅ 已处理（03 回改） |

---

## D. 建议优化项

| 编号 | 建议 | 优先级 | 说明 |
|------|------|--------|------|
| D-01 | docs/12 §3.1 命令记录应标注"此为过渡命令，最终 net8.0" | 低 | 避免后续读者误以为项目是 net6.0 |
| D-02 | AppDbContext.cs 中 Reservation 的外键删除行为应显式配置为 `DeleteBehavior.Restrict` | 中 | 与 docs/08 设计口径一致，防止级联删除 |
| D-03 | docs/12 §3.3 的"9 个页面视图"应改为"2 个完整页 + 7 个骨架页" | 低 | 更准确反映 Sprint 0 实际产出 |
| D-04 | README 中"Sprint 0"表格应保留，但后续 Sprint 可标注为"已完成"而非仅列当前阶段 | 低 | 便于项目历史追溯 |

---

## E. 门禁逐项复核

### 1. docs/11 P0 是否清零 ✅

docs/11 §9 列出 3 项 P0 阻塞项，任务板 §12 "清零状态跟踪"表显示全部 ✅ 已处理。本轮 Sprint 0 未发现新增 P0。

### 2. 工程结构是否清楚 ✅

```
src/LibrarySeatReservation.Web/
├── Controllers/        # 4 个 Controller
├── Models/Entities/    # 3 个 Entity
├── Data/               # DbContext + SeedData
├── DataAccess/         # 3 个接口 + 3 个实现
├── Services/           # 3 个接口 + 3 个实现
├── Helpers/            # PasswordHelper
├── Constants/          # TimeSlots
├── Views/              # 10 个 .cshtml
├── Migrations/         # InitialCreate
└── wwwroot/            # 静态资源
```

目录职责清晰，与 docs/07 §4.1 分层设计一致。

### 3. EF Core + SQL Server LocalDB 接入 ✅

- csproj 包含 `Microsoft.EntityFrameworkCore.SqlServer 8.0.0` 和 `Tools 8.0.0`
- appsettings.json 连接字符串指向 `(localdb)\mssqllocaldb`，数据库名 `LibrarySeatReservationDb`
- Program.cs 使用 `options.UseSqlServer(...)` 注册

### 4. Program.cs 注册合理性 ✅

| 注册项 | 内容 | 评估 |
|--------|------|------|
| DbContext | SQL Server LocalDB | ✅ 正确 |
| MemoryCache | Sprint 3 引入 | ✅ 正确（本轮审计范围外但不阻塞） |
| Session | 30分钟超时，HttpOnly，Essential | ✅ 与 docs/07 §9.1 一致 |
| DI | 3 Repository + 3 Service（Scoped） | ✅ 符合 Web 应用生命周期 |

### 5. DbContext / Entity / Service / DataAccess 骨架 ✅

- **Entity**：Seat（6字段）、Reservation（7字段+导航属性）、AdminUser（4字段）— 与 docs/08 §4 一致
- **DbContext**：3 个 DbSet + 索引配置（Seat.Name 唯一、Reservation 复合唯一、AdminUser.Username 唯一）— 与 docs/08 §9 一致
- **DataAccess**：3 个接口 + 3 个实现，CRUD 方法完整
- **Service**：3 个接口 + 3 个实现，业务逻辑封装完整

### 6. 迁移与建库建表 ✅

- `Migrations/` 目录存在，含 `InitialCreate` 迁移
- docs/12 §3.8 记录了 `dotnet ef migrations add InitialCreate` + `dotnet ef database update` 成功
- Program.cs 启动时自动 `context.Database.Migrate()`

### 7. 种子数据 ✅

| 表 | 数量 | 内容 |
|----|------|------|
| Seats | 20 条 | A/B/C/D 区各 5 个（含 2 个维护中 Status=1） |
| AdminUsers | 1 条 | admin/123456（PasswordHasher 哈希存储） |
| Reservations | 0 条 | 待 Sprint 1 产生 |

- 种子数据能支撑：体验账号切换（张三/李四/王五）、管理员登录（admin/123456）、首页可见数据
- SeedData.Initialize() 使用 `if (context.Seats.Any()) return;` 防重复初始化
- 种子数据初始化方式与最终交付口径不冲突（最终也是20座位+1管理员）

### 8. 命名空间/目录/实体/路由与 docs/07/08/09 一致性 ✅

| 检查项 | 设计文档口径 | 实际实现 | 一致性 |
|--------|-------------|---------|--------|
| 实体命名 | Seat, Reservation, AdminUser | Seat, Reservation, AdminUser | ✅ |
| Controller 命名 | HomeController, SeatController, ReservationController, AdminController | 同左 | ✅ |
| 路由 | /Seat/List, /Seat/Detail/{id}, /Reservation/Create, /Reservation/My, /Admin/Login, /Admin/SeatIndex, /Admin/ReservationIndex, /Admin/Statistics | 同左 | ✅ |
| Session Key | CurrentUserName, AdminUsername | 同左 | ✅ |
| 时段常量 | Morning1~Evening | 同左 | ✅ |

### 9. 首页/用户端入口/管理端入口可访问 ✅

- 首页 `/` → HomeController.Index → 完整视图（欢迎语+导航+体验账号切换）
- 用户端入口：导航栏含"座位列表""我的预约"链接
- 管理端入口：导航栏含"管理端"链接 → `/Admin/Login`

### 10. 默认补足项显式写出 ✅

docs/12 §4 列出 6 项假设/补足项，每项均标注了原因（"用户明确指定"或"前序文档未指定"），补得合理。

### 11. 任务板 Sprint 0 状态更新 ✅

任务板 §13 记录 T17-01~T17-10 全部 ✅ 已完成，Sprint 概况显示完成率 100%。

### 12. README 增量更新 ✅

README 包含：
- 当前阶段：Sprint 0~4 全部完成
- 运行步骤：`dotnet run --urls http://localhost:5000`
- 数据库初始化：自动 Migrate + SeedData
- 种子数据/演示账号：张三/李四/王五 + admin/123456
- 已完成范围：10 页功能清单
- 后续计划：无（Sprint 4 已完成）

### 13. 编译与运行目标 ✅

| 目标 | 状态 |
|------|------|
| 能编译 | ✅ `dotnet build` 0 errors |
| 能启动 | ✅ `dotnet run` 可访问 |
| 能看到入口页 | ✅ 首页有完整视图 |
| 能建库建表 | ✅ 自动 Migrate + 迁移文件存在 |
| 能种子初始化 | ✅ 20 座位 + 1 管理员 |

---

## F. 结论

**✅ 通过，可进入下一步"用户端主链路开发"。**

Sprint 0 骨架搭建产物完整、docs/11 P0 已清零、工程结构与设计文档一致、编译通过、数据库已建表、种子数据已初始化。B-01~B-05 均为 P1/P2 级建议优化项，不阻塞后续开发。
