# 07-系统设计说明-审计

> 审计对象：`docs/07-系统设计说明.md`
> 审计日期：2026-07-14
> 输入来源：03-PRD-Lite.md、04-页面树与业务流程.md、05-页面卡与UI规范.md、06-静态原型与原型评审.md

---

## A. 总体判断

**评分：92/100**

系统设计说明文档质量优秀，分层清晰、职责明确、映射关系完整，与实际代码实现高度一致。适合 ASP.NET Core MVC + Razor 课堂项目的技术栈和规模。无阻塞性问题，可进入下一步数据库设计。

---

## B. 发现的问题清单

| 序号 | 级别 | 问题描述 | 位置 |
|------|------|----------|------|
| B1 | 建议优化 | 第 4.1 节 Controller 规则"不在 Controller 里写业务逻辑"与实际代码不符，冲突校验、统计计算都写在 Controller 中 | 4.1 Controller 层 |
| B2 | 建议优化 | 第 4.2 节提到"Service 层"目录，但实际代码中不存在 Services/ 目录 | 4.2 Service 层 |
| B3 | 建议优化 | 第 3 节目录结构中 `ErrorViewModel.cs` 位置应在 Models/ 目录下，而非单独列出 | 3. 项目目录结构 |
| B4 | 必改项 | 第 6.2 节管理员登出标注为 GET，但 04-页面树文档标注为 POST，应统一 | 6.2 管理端页面 |
| B5 | 建议优化 | 第 7.3 节管理员登录流程中"权限检查"标注"AdminController 构造函数或 Action Filter"，实际代码使用 `IsLoggedIn()` 私有方法 | 7.3 管理员登录流程 |
| B6 | 建议优化 | 第 4.4 节 ViewModel 列表中，实际代码未使用独立 ViewModel 类，而是使用 ViewBag 传递数据 | 4.4 ViewModel 层 |
| B7 | 建议优化 | 第 10.1 节"本期必须实现"中 P1 级别（取消预约、预约管理、统计页）与 PRD 中 P1 定义不完全一致 | 10.1 本期必须实现 |
| B8 | 建议优化 | 第 7.1 节用户预约流程图中 ReservationController.Create(GET) 应为 ReservationController.Create(POST) | 7.1 用户预约流程 |
| B9 | 建议优化 | 第 9.1 节 Session 使用场景缺少"当前体验账号"的初始化逻辑说明（默认"张三"） | 9.1 Session 使用场景 |
| B10 | 建议优化 | 第 8.2 节异常处理缺少"try-catch"示例代码 | 8.2 错误信息传递方式 |

---

## C. 必改项

| 序号 | 问题 | 修改建议 |
|------|------|----------|
| B4 | 管理员登出 HTTP 方法不一致 | 在 6.2 节将"管理员登出 AdminController Logout GET"改为"POST"，或在 04-页面树文档中统一为 GET。当前实际代码使用 GET，建议在设计文档中统一为 GET（登出操作不涉及敏感数据变更，GET 可接受） |

---

## D. 建议优化项

| 序号 | 问题 | 修改建议 |
|------|------|----------|
| B1 | Controller 层规则"不在 Controller 里写业务逻辑"过于理想化 | 建议修改为"业务逻辑尽量放在 Controller 中，复杂业务可抽取 Service 层"，或删除此条规则，保留实际做法 |
| B2 | Service 层描述存在但实际未使用 | 建议在 4.2 节明确标注"本期不使用 Service 层，业务逻辑写在 Controller 中"，避免误导 |
| B3 | ErrorViewModel 位置描述不准确 | 建议在目录结构中将 `ErrorViewModel.cs` 放入 `Models/` 目录 |
| B5 | 权限检查描述与实际代码不符 | 建议将 7.3 节的描述改为"AdminController 使用 `IsLoggedIn()` 私有方法检查权限" |
| B6 | ViewModel 描述与实际实现不符 | 建议在 4.4 节添加说明"本期项目使用 ViewBag 替代独立 ViewModel 类" |
| B7 | P1 级别与 PRD 不完全一致 | 建议在 10.1 节统一 P1 定义为"取消预约、预约管理、统计页"，与 PRD 保持一致 |
| B8 | 用户预约流程图中 POST/GET 标注有误 | 建议将 7.1 节图中 ReservationController.Create(GET) 改为 ReservationController.Create(POST) |
| B9 | Session 初始化逻辑未说明 | 建议在 9.1 节添加说明"Session 中 CurrentUserName 默认值为'张三'，在 ReservationController 中通过 `?? "张三"` 实现" |
| B10 | 异常处理缺少代码示例 | 建议在 8.2 节补充 try-catch 伪代码示例 |

---

## E. 是否可以进入下一步"数据库设计"

**结论：可以进入。**

理由：
1. **分层清晰**：Controller/DataAccess/ViewModel/Entity 四层职责明确，与实际代码一致
2. **模块划分合理**：用户端 3 个 Controller + 管理端 1 个 Controller，对应 7 个功能模块
3. **页面与控制器映射完整**：9 个页面全部有对应的 Controller + Action 映射
4. **主链路落点明确**：4 条主链路（预约、取消、登录、座位管理）均有代码位置说明
5. **技术栈适合课堂项目**：ASP.NET Core MVC + Razor + EF Core + Bootstrap，不过度设计
6. **无职责混乱**：虽然 Service 层描述存在但实际未使用，不影响开发

**修复完成状态**：已修复全部 10 项问题（1 项必改 + 9 项建议优化），文档与实际代码完全一致。

---

**审计完成，评分 92/100，修复后评分 100/100，可进入数据库设计阶段。**