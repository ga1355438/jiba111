# 17-交付说明与项目复盘-审计

> 文档类型：阶段 17 交付审计报告
> 审计对象：README.md、database/、docs/17-交付说明与项目复盘.md、仓库整体结构
> 审计日期：2026-07-14
> 基线：当前仓库真实状态（commit 4e55a6e, tag v2.1-final）

---

## A. 总体判断

**基本通过，有 1 个必改项**。README 功能清单、页面清单、种子数据、运行步骤基本忠实反映当前仓库真实实现，database/ 文档清晰完整。但 docs/17 中最终 commit hash 和 tag 标注与实际仓库不一致，属于事实性错误，必须修正。仓库中仍保留旧版 `LibrarySeatSystem/` 目录（105 个 tracked 文件），建议删除。

---

## B. 发现的问题清单

### B-1. docs/17 最终 commit hash 和 tag 标注错误（P0-必改）

**文件**：`docs/17-交付说明与项目复盘.md:6,136`

**问题**：
- 写"最终 commit：dabd2dc"，实际最新 commit 为 `4e55a6e`
- 写"Tag：v2.1"，实际最终 tag 为 `v2.1-final`

**影响**：任何人按 docs/17 的 commit hash 或 tag 去验证，都会找不到对应提交。

**修复**：更新为实际值。

---

### B-2. LibrarySeatSystem/ 旧版目录仍在仓库中（P1-建议删除）

**路径**：`LibrarySeatSystem/`

**问题**：旧版 .NET 6 项目，105 个 tracked 文件，已被 `src/LibrarySeatReservation.Web/` 完全替代。docs/17 §4.2 已记录此问题并建议删除，但实际未执行。

**影响**：仓库体积膨胀，老师/同学可能混淆新旧版本。

**建议**：`git rm -r LibrarySeatSystem/` 删除。

---

### B-3. README 管理端页面数表述可更精确（P2-建议优化）

**文件**：`README.md:93`

**问题**：README 写"管理端（5 页）"，实际 Admin 目录下有 6 个视图文件（Login、SeatIndex、SeatCreate、SeatEdit、ReservationIndex、Statistics）。README 将 SeatCreate+SeatEdit 合并为一行"新增/编辑座位"，这是合理的功能分组，但严格来说管理端有 6 个独立视图。

**影响**：不影响验收，但如果老师逐文件核对可能产生疑问。

**建议**：可保持现状（功能分组合理），或改为"管理端（6 个视图，功能分组为 5 行）"。

---

### B-4. Home/Privacy.cshtml 未在页面清单中提及（P2-建议优化）

**文件**：`Views/Home/Privacy.cshtml`

**问题**：此视图文件存在但未在 README 页面清单中列出。这是 .NET MVC 模板默认生成的隐私政策页面，非功能性页面。

**影响**：不影响验收，但严格来说仓库中多了一个未文档化的视图。

**建议**：可保持现状（模板默认页面，无需文档化），或在 README 项目结构中加一句注释。

---

### B-5. prototype/ 目录内容未在 README 中说明（P2-建议优化）

**文件**：`README.md:129`

**问题**：README 项目结构中提到 `prototype/` 但未说明其内容（11 个 HTML 静态原型文件）。

**影响**：不影响验收。

**建议**：可保持现状。

---

## C. 必改项

| 编号 | 问题 | 修复建议 |
|------|------|---------|
| B-1 | docs/17 commit hash 和 tag 标注错误 | 更新为 `4e55a6e` 和 `v2.1-final` |

---

## D. 建议优化项

| 编号 | 问题 | 修复建议 |
|------|------|---------|
| B-2 | LibrarySeatSystem/ 旧版目录仍在 | `git rm -r LibrarySeatSystem/` |
| B-3 | 管理端页面数表述 | 可保持现状或加注释 |
| B-4 | Privacy.cshtml 未文档化 | 模板默认页面，可不处理 |
| B-5 | prototype/ 内容未说明 | 可保持现状 |

---

## E. 是否可以作为最终提交版本

**可以，但建议先修复 B-1**。

B-1 是事实性错误（commit hash 和 tag 写错），修复后即可作为最终提交版本。B-2 建议删除以保持仓库干净，但不阻塞验收。

### 修复 B-1 后的验证清单

- [ ] docs/17 最终 commit 更新为 `4e55a6e`
- [ ] docs/17 Tag 更新为 `v2.1-final`
- [ ] git add + commit + push

---

*审计完毕。*
